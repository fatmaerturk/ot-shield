# Extract CEF extension key=value pairs where a value runs until the next
# " key=" (so multi-word values like country names / attack types stay whole).
[cef_kv]
REGEX = (\w+)=(.*?)(?=\s+\w+=|\s*$)
FORMAT = $1::$2
