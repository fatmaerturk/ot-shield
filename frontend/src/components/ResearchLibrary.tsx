/**
 * @deprecated Use {@code ResearchStudio} instead. This module is kept
 * only so any in-flight branches that still import
 * {@code ResearchLibrary} keep compiling; it forwards to the new shell
 * which renders the Library tab by default.
 */
export { default } from './ResearchStudio';
